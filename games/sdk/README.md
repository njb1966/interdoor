# InterDoor SDK Placeholder

This directory is reserved for future SDK documentation, examples, or shared integration helpers.

For now, the active SDK contract is described in `../../05_Game_SDK.md`, and the working Go reference lives under `../ledgerofthelow/internal/engine` and `../ledgerofthelow/internal/fed`.

Current Phase 1 non-Go examples:

- `tier1_http.md`: minimum register and heartbeat flow for a listed node.
- `tier2_http.md`: event and roster push/pull flow for a network-aware node.
